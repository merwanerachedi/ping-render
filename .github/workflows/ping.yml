name: Ping Render Service

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render endpoint
        run: |
          curl -s https://discord-pokemon-bot-8ch9.onrender.com
