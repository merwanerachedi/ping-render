name: Ping Render Service

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render endpoint
        run: |
          curl -s ${{ secrets.RENDER_URL }}
